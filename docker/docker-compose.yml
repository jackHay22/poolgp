version: '3'

services:
  eval:
    deploy:
      replicas: 6
    image: jackhay22/poolgp_server:latest
    ports:
      - "9999:9999"
  engine:
    image: jackhay22/poolgp_clojush:latest
    deploy:
      replicas: 1
    depends_on:
      - eval
    ports:
      - "8000:8000"
      - "9000:9000"
